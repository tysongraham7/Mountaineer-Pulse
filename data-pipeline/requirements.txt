requests>=2.31.0
python-dotenv>=1.0.0
supabase>=2.20.0
anthropic>=0.109.0
websockets>=13
# zoneinfo has no timezone database of its own on Windows, so every script using
# ZoneInfo (notify_news, notify_games, generate_briefing) dies there without this.
# Linux runners have system tzdata, which is why CI never noticed.
tzdata

# migrate.py only. It talks to Postgres directly rather than through PostgREST, because
# CREATE TABLE and ALTER TABLE have no REST equivalent. Not used by any workflow — schema
# changes are applied by hand — but every script here shares one requirements file.
psycopg2-binary
